# M20 — Power-Ups In-Run Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar drops de power-ups temporales en-run: enemigos sueltan power-ups al morir (15% flat), el jugador los recoge por contacto, efectos temporales gestionados por RunManager con timers, y el HUD muestra los power-ups activos con countdown.

**Architecture:** `UpgradeData` se amplía con campos `is_temporary/effect_type/duration/magnitude`. `LootTable` (stub estático) intercepta la muerte de enemigos y decide el drop. `PowerUpPickup` (Area2D) flota hacia la izquierda y notifica a `EventBus` al ser tocada. `RunManager` aplica efectos al Player vía `apply_powerup()` y los expira via `SceneTreeTimer`.

**Tech Stack:** Godot 4.6, GDScript tipado estricto, EventBus (Autoload), BulletPoolManager no involucrado (drops son instancias dinámicas raras).

---

## Mapa de Archivos

| Acción | Archivo | Responsabilidad |
|--------|---------|-----------------|
| Modificar | `scripts/data/upgrade_data.gd` | Añadir 4 campos para power-ups temporales |
| Modificar | `autoloads/event_bus.gd` | Añadir `power_up_collected` y `power_up_expired` |
| Crear | `scenes/entities/power_up_pickup.gd` | Area2D que deriva, detecta Player y emite señal |
| Crear | `scenes/entities/PowerUpPickup.tscn` | Area2D + ColorRect + CollisionShape2D |
| Crear | `resources/upgrades/pu-rapid-fire.tres` | Resource de datos del power-up |
| Crear | `resources/upgrades/pu-speed-boost.tres` | Resource de datos del power-up |
| Crear | `resources/upgrades/pu-shield-pulse.tres` | Resource de datos del power-up |
| Crear | `resources/upgrades/pu-scrap-magnet.tres` | Resource de datos del power-up |
| Crear | `resources/upgrades/pu-wave-amp.tres` | Resource de datos del power-up |
| Crear | `scripts/gameplay/loot_table.gd` | Stub: 15% drop, pool fijo, instancia pickup |
| Modificar | `scenes/entities/enemy_base.gd` | Llamar `LootTable.try_drop()` en la muerte |
| Modificar | `scenes/entities/player.gd` | `apply_powerup()`, `revert_powerup()`, estados de efecto |
| Modificar | `autoloads/run_manager.gd` | `active_power_ups`, timers, conectar señales |
| Modificar | `scenes/ui/hud.gd` | Panel de power-ups activos con countdown |
| Modificar | `scenes/ui/HUD.tscn` | Añadir nodo `PowerUpPanel` (HBoxContainer) |

---

## Task 1: Extender `UpgradeData`

**Files:**
- Modify: `scripts/data/upgrade_data.gd`

- [ ] **Step 1: Añadir los 4 campos nuevos a UpgradeData**

Reemplazar todo el contenido del archivo:

```gdscript
class_name UpgradeData
extends Resource

## Base de datos atómica de la Tienda. Separa datos mercantiles
## de la lógica del personaje. (TDD §1.1)
## M20: Ampliado con campos de power-ups temporales in-run.

@export var id: String = ""
@export var cost_scrap: int = 0
@export var icon: Texture2D
@export var stat_modifier_dict: Dictionary = {}

## M20 — Power-Up In-Run fields
@export var is_temporary: bool = false
## Identificador del efecto: pu_rapid_fire, pu_speed_boost, pu_shield_pulse,
## pu_scrap_magnet, pu_wave_amp
@export var effect_type: StringName = &""
## Duración en segundos. -1.0 = toda la run (no expira)
@export var duration: float = -1.0
## Multiplicador del efecto (semántica depende de effect_type)
@export var magnitude: float = 1.0
```

- [ ] **Step 2: Verificar compilación**

Abre el proyecto en Godot. En la pestaña `Output`, verifica que no aparece ningún error de parseo para `upgrade_data.gd`. Si hay errores de tipado, revisa que los `@export` usen los tipos correctos (`StringName`, `float`, `bool`).

- [ ] **Step 3: Commit**

```bash
git add scripts/data/upgrade_data.gd
git commit -m "feat(M20): extend UpgradeData with is_temporary, effect_type, duration, magnitude"
```

---

## Task 2: Añadir señales al EventBus

**Files:**
- Modify: `autoloads/event_bus.gd`

- [ ] **Step 1: Añadir las dos señales nuevas**

Añadir al final de `autoloads/event_bus.gd`:

```gdscript
signal power_up_collected(upgrade: UpgradeData)
signal power_up_expired(upgrade: UpgradeData)
```

El archivo completo debe quedar:

```gdscript
extends Node

signal enemy_destroyed(score_value: int, position: Vector2)
signal boss_phase_changed(new_phase: int)
signal boss_defeated()
signal player_shoot()
signal player_died()
signal wave_charge_started()
signal wave_charge_changed(level: int)
signal wave_cannon_fired(level: int, power: float)
signal wave_cannon_cancelled()
signal force_state_changed(new_state: int)
signal force_hit_enemy(damage: int)
signal biome_transition_requested(profile: Resource)
signal power_up_collected(upgrade: UpgradeData)
signal power_up_expired(upgrade: UpgradeData)
```

- [ ] **Step 2: Verificar compilación**

En `Output` de Godot, sin errores de parseo en `event_bus.gd`.

- [ ] **Step 3: Commit**

```bash
git add autoloads/event_bus.gd
git commit -m "feat(M20): add power_up_collected and power_up_expired signals to EventBus"
```

---

## Task 3: Crear `PowerUpPickup.tscn` y su script

**Files:**
- Create: `scenes/entities/power_up_pickup.gd`
- Create: `scenes/entities/PowerUpPickup.tscn`

- [ ] **Step 1: Crear el script `power_up_pickup.gd`**

```gdscript
# scenes/entities/power_up_pickup.gd
class_name PowerUpPickup
extends Area2D

const DRIFT_SPEED: float = 60.0

## Color visual por effect_type — visual placeholder hasta M21 (sprites reales)
const EFFECT_COLORS: Dictionary = {
	&"pu_rapid_fire":   Color(1.0, 0.5, 0.0),
	&"pu_speed_boost":  Color(0.0, 1.0, 1.0),
	&"pu_shield_pulse": Color(0.2, 0.6, 1.0),
	&"pu_scrap_magnet": Color(0.8, 1.0, 0.2),
	&"pu_wave_amp":     Color(1.0, 0.2, 1.0),
}

var upgrade_data: UpgradeData

@onready var _rect: ColorRect = $ColorRect


## Llamar antes de add_child() para inyectar el resource.
func init(data: UpgradeData) -> void:
	upgrade_data = data


func _ready() -> void:
	if upgrade_data != null:
		_rect.color = EFFECT_COLORS.get(upgrade_data.effect_type, Color.WHITE)
	body_entered.connect(_on_body_entered)


func _process(delta: float) -> void:
	position.x -= DRIFT_SPEED * delta
	if global_position.x < -100.0:
		queue_free()


func _on_body_entered(body: Node2D) -> void:
	if body is Player:
		EventBus.power_up_collected.emit(upgrade_data)
		queue_free()
```

- [ ] **Step 2: Crear la escena `PowerUpPickup.tscn`**

```
[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scenes/entities/power_up_pickup.gd" id="1"]

[sub_resource type="CircleShape2D" id="CircleShape2D_1"]
radius = 16.0

[node name="PowerUpPickup" type="Area2D"]
collision_layer = 0
collision_mask = 1
script = ExtResource("1")

[node name="ColorRect" type="ColorRect" parent="."]
offset_left = -8.0
offset_top = -8.0
offset_right = 8.0
offset_bottom = 8.0
color = Color(1, 1, 1, 1)

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("CircleShape2D_1")
```

- [ ] **Step 3: Verificar en Godot**

Abre `PowerUpPickup.tscn` en el editor. Debe mostrar un nodo `Area2D` con `ColorRect` de 16×16 px y `CollisionShape2D` circular. En `Inspector`, verificar que `Collision Layer = 0` y `Collision Mask = Layer 1 (Jugador_Nucleo)`.

- [ ] **Step 4: Commit**

```bash
git add scenes/entities/power_up_pickup.gd scenes/entities/PowerUpPickup.tscn
git commit -m "feat(M20): add PowerUpPickup Area2D scene — drifts left, auto-collects on Player contact"
```

---

## Task 4: Crear los 5 recursos `.tres`

**Files:**
- Create: `resources/upgrades/pu-rapid-fire.tres`
- Create: `resources/upgrades/pu-speed-boost.tres`
- Create: `resources/upgrades/pu-shield-pulse.tres`
- Create: `resources/upgrades/pu-scrap-magnet.tres`
- Create: `resources/upgrades/pu-wave-amp.tres`

- [ ] **Step 1: Crear `pu-rapid-fire.tres`**

```
[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/upgrade_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "pu_rapid_fire"
cost_scrap = 0
stat_modifier_dict = {}
is_temporary = true
effect_type = &"pu_rapid_fire"
duration = 10.0
magnitude = 1.5
```

- [ ] **Step 2: Crear `pu-speed-boost.tres`**

```
[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/upgrade_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "pu_speed_boost"
cost_scrap = 0
stat_modifier_dict = {}
is_temporary = true
effect_type = &"pu_speed_boost"
duration = 8.0
magnitude = 1.3
```

- [ ] **Step 3: Crear `pu-shield-pulse.tres`**

```
[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/upgrade_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "pu_shield_pulse"
cost_scrap = 0
stat_modifier_dict = {}
is_temporary = true
effect_type = &"pu_shield_pulse"
duration = -1.0
magnitude = 1.0
```

- [ ] **Step 4: Crear `pu-scrap-magnet.tres`**

```
[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/upgrade_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "pu_scrap_magnet"
cost_scrap = 0
stat_modifier_dict = {}
is_temporary = true
effect_type = &"pu_scrap_magnet"
duration = 15.0
magnitude = 3.0
```

- [ ] **Step 5: Crear `pu-wave-amp.tres`**

```
[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/data/upgrade_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "pu_wave_amp"
cost_scrap = 0
stat_modifier_dict = {}
is_temporary = true
effect_type = &"pu_wave_amp"
duration = 12.0
magnitude = 1.0
```

- [ ] **Step 6: Verificar carga**

En Godot, abre el FileSystem y haz doble click en uno de los `.tres`. Debe abrirse en el Inspector mostrando todos los campos correctamente. Si aparece "Unknown class" o campos vacíos, verifica que `scripts/data/upgrade_data.gd` esté parseado correctamente (Task 1).

- [ ] **Step 7: Commit**

```bash
git add resources/upgrades/
git commit -m "feat(M20): add 5 power-up .tres resources (rapid-fire, speed-boost, shield-pulse, scrap-magnet, wave-amp)"
```

---

## Task 5: Crear `LootTable` (stub)

**Files:**
- Create: `scripts/gameplay/loot_table.gd`

- [ ] **Step 1: Crear el script**

```gdscript
# scripts/gameplay/loot_table.gd
## Stub M20: drop plano 15%. M21/M22 inyectarán pesos por heat level
## sin modificar EnemyBase — solo extender este archivo.
class_name LootTable

const DROP_CHANCE: float = 0.15

## Pool fijo M20. M21: reemplazar por Array ponderado por heat.
const _POWER_UP_PATHS: Array[String] = [
	"res://resources/upgrades/pu-rapid-fire.tres",
	"res://resources/upgrades/pu-speed-boost.tres",
	"res://resources/upgrades/pu-shield-pulse.tres",
	"res://resources/upgrades/pu-scrap-magnet.tres",
	"res://resources/upgrades/pu-wave-amp.tres",
]

const _PICKUP_SCENE_PATH: String = "res://scenes/entities/PowerUpPickup.tscn"


## Punto de entrada desde EnemyBase. M21/M22: añadir parámetro heat_level: int = 0.
static func try_drop(position: Vector2, parent: Node) -> void:
	if randf() > DROP_CHANCE:
		return
	var idx: int = randi() % _POWER_UP_PATHS.size()
	var data: UpgradeData = load(_POWER_UP_PATHS[idx]) as UpgradeData
	if data == null:
		push_error("[LootTable] Failed to load power-up at index %d" % idx)
		return
	var scene: PackedScene = load(_PICKUP_SCENE_PATH) as PackedScene
	var pickup: PowerUpPickup = scene.instantiate() as PowerUpPickup
	pickup.init(data)
	parent.add_child(pickup)
	pickup.global_position = position
```

- [ ] **Step 2: Verificar compilación**

Sin errores en `Output`. Confirmar que `class_name LootTable` aparece como clase disponible en el autocompletado del editor.

- [ ] **Step 3: Commit**

```bash
git add scripts/gameplay/loot_table.gd
git commit -m "feat(M20): add LootTable stub — 15% flat drop, fixed pool, ready for heat-weighted M21 extension"
```

---

## Task 6: Modificar `EnemyBase` para llamar `LootTable`

**Files:**
- Modify: `scenes/entities/enemy_base.gd`

- [ ] **Step 1: Insertar llamada a LootTable en `take_damage()`**

En `enemy_base.gd`, localiza la función `take_damage`. La línea `_spawn_explosion()` está antes de `EventBus.enemy_destroyed.emit(...)`. Añadir la llamada a `LootTable.try_drop()` entre `_spawn_explosion()` y `queue_free()`.

Reemplazar solo el bloque del if dentro de `take_damage`:

```gdscript
func take_damage(amount: int) -> void:
	_current_hp -= amount
	if _current_hp <= 0:
		_spawn_explosion()
		LootTable.try_drop(global_position, get_parent())
		EventBus.enemy_destroyed.emit(profile.point_value, global_position)
		queue_free()
		return
	_hit_material.set_shader_parameter("hit_flash", true)
	var tween: Tween = create_tween()
	tween.tween_callback(_clear_flash).set_delay(0.05)
```

- [ ] **Step 2: Verificar en juego**

Lanza el proyecto (`F5`). Mata varios enemigos (dispara repetidamente). Observa que aproximadamente 1 de cada 7 muertes genera un cuadrado de color que se desplaza hacia la izquierda. Si el pickup no aparece, comprueba el `Output` por errores de `[LootTable]`.

- [ ] **Step 3: Commit**

```bash
git add scenes/entities/enemy_base.gd
git commit -m "feat(M20): EnemyBase calls LootTable.try_drop() on death — 15% power-up drop chance"
```

---

## Task 7: Extender `Player` con `apply_powerup` / `revert_powerup`

**Files:**
- Modify: `scenes/entities/player.gd`

- [ ] **Step 1: Añadir variables de estado para los efectos**

Después de la línea `var _charge_level: int = 0`, añadir:

```gdscript
## M20 — Power-Up state
var _base_normal_speed: float = 0.0
var _rapid_fire_active: bool = false
var _rapid_fire_timer: float = 0.0
var _has_shield: bool = false
var _wave_amp_active: bool = false

const RAPID_FIRE_INTERVAL: float = 0.15
```

- [ ] **Step 2: Inicializar `_base_normal_speed` en `_ready()`**

Al final de la función `_ready()`, añadir:

```gdscript
	_base_normal_speed = normal_speed
```

- [ ] **Step 3: Modificar `_handle_fire_normal` para rapid fire**

Reemplazar la función completa:

```gdscript
func _handle_fire_normal(delta: float) -> void:
	if Input.is_action_just_pressed("fire"):
		_shoot()
		_hold_time = 0.0
		_rapid_fire_timer = RAPID_FIRE_INTERVAL
	elif Input.is_action_pressed("fire"):
		_hold_time += delta
		if _rapid_fire_active:
			_rapid_fire_timer -= delta
			if _rapid_fire_timer <= 0.0:
				_shoot()
				_rapid_fire_timer = RAPID_FIRE_INTERVAL
		if _hold_time >= CHARGE_START_THRESHOLD:
			_enter_charging()
	elif Input.is_action_just_released("fire"):
		_hold_time = 0.0
		_rapid_fire_timer = RAPID_FIRE_INTERVAL
```

- [ ] **Step 4: Modificar `take_damage` para el escudo**

Reemplazar la función completa:

```gdscript
func take_damage(amount: int) -> void:
	if _state == State.DEAD:
		return
	if _has_shield:
		_has_shield = false
		RunManager.notify_shield_consumed()
		return
	_current_hp -= amount
	if _current_hp <= 0:
		_die()
```

- [ ] **Step 5: Modificar `_release_wave_cannon` para wave amp**

Reemplazar solo el bloque del if `_charge_level == 0`:

```gdscript
func _release_wave_cannon() -> void:
	_state = State.NORMAL

	if _charge_level == 0:
		if _wave_amp_active:
			_charge_level = 1
		else:
			EventBus.wave_cannon_cancelled.emit()
			print("[WaveCannon] Released too early — no fire (%.2fs)" % _charge_time)
			_charge_time = 0.0
			return

	var power: float = minf(_charge_time / CHARGE_THRESHOLDS[2], 1.0)
	EventBus.wave_cannon_fired.emit(_charge_level, power)
	print("[WaveCannon] FIRED level=%d | power=%.2f | t=%.2fs" % [_charge_level, power, _charge_time])

	var _w: Node = BulletPoolManager.get_wave_bullet(
		global_position + MUZZLE_OFFSET,
		_charge_level,
		700.0,
		WaveBullet.DAMAGE_BY_LEVEL[_charge_level - 1]
	)

	_charge_time = 0.0
	_charge_level = 0
```

- [ ] **Step 6: Añadir `apply_powerup` y `revert_powerup` al final del script**

```gdscript
## Aplica el efecto de un power-up. Llamado por RunManager.
func apply_powerup(data: UpgradeData) -> void:
	match data.effect_type:
		&"pu_rapid_fire":
			_rapid_fire_active = true
		&"pu_speed_boost":
			normal_speed = _base_normal_speed * data.magnitude
		&"pu_shield_pulse":
			_has_shield = true
		&"pu_wave_amp":
			_wave_amp_active = true
		&"pu_scrap_magnet":
			print("[Player] pu_scrap_magnet stub — M21 implementará radio de recolección")


## Revierte el efecto de un power-up expirado. Llamado por RunManager.
func revert_powerup(data: UpgradeData) -> void:
	match data.effect_type:
		&"pu_rapid_fire":
			_rapid_fire_active = false
			_rapid_fire_timer = 0.0
		&"pu_speed_boost":
			normal_speed = _base_normal_speed
		&"pu_shield_pulse":
			_has_shield = false
		&"pu_wave_amp":
			_wave_amp_active = false
		&"pu_scrap_magnet":
			pass
```

- [ ] **Step 7: Verificar compilación**

Sin errores en `Output`. El juego debe iniciar sin crashes.

- [ ] **Step 8: Commit**

```bash
git add scenes/entities/player.gd
git commit -m "feat(M20): Player.apply_powerup/revert_powerup — rapid fire, speed boost, shield, wave amp"
```

---

## Task 8: Extender `RunManager` con timers de power-ups

**Files:**
- Modify: `autoloads/run_manager.gd`

- [ ] **Step 1: Reescribir `run_manager.gd` completo**

```gdscript
extends Node

signal score_changed(new_score: int)

var current_score: int = 0
var active_power_ups: Array[UpgradeData] = []

var _power_up_timers: Dictionary = {}  # StringName → SceneTreeTimer


func _ready() -> void:
	EventBus.enemy_destroyed.connect(_on_enemy_destroyed)
	EventBus.power_up_collected.connect(_on_power_up_collected)


func _on_enemy_destroyed(score_value: int, _position: Vector2) -> void:
	add_score(score_value)


func add_score(value: int) -> void:
	current_score += value
	score_changed.emit(current_score)


func reset() -> void:
	current_score = 0
	active_power_ups.clear()
	_power_up_timers.clear()
	score_changed.emit(0)


func _on_power_up_collected(data: UpgradeData) -> void:
	var player: Player = _get_player()
	if player == null:
		push_warning("[RunManager] power_up_collected but Player not found in scene")
		return
	player.apply_powerup(data)
	active_power_ups.append(data)
	if data.duration > 0.0:
		var timer: SceneTreeTimer = get_tree().create_timer(data.duration)
		_power_up_timers[data.effect_type] = timer
		timer.timeout.connect(_expire_power_up.bind(data))


func _expire_power_up(data: UpgradeData) -> void:
	var player: Player = _get_player()
	if player != null:
		player.revert_powerup(data)
	active_power_ups.erase(data)
	_power_up_timers.erase(data.effect_type)
	EventBus.power_up_expired.emit(data)


## Llamado por Player cuando pu_shield_pulse absorbe un golpe (duración -1, sin timer).
## Limpia active_power_ups y notifica al HUD vía EventBus.
func notify_shield_consumed() -> void:
	for i: int in active_power_ups.size():
		if active_power_ups[i].effect_type == &"pu_shield_pulse":
			var data: UpgradeData = active_power_ups[i]
			active_power_ups.remove_at(i)
			EventBus.power_up_expired.emit(data)
			return


## Devuelve el tiempo restante de un power-up activo. 0.0 si no existe o es permanente.
func get_power_up_time_left(effect_type: StringName) -> float:
	if not _power_up_timers.has(effect_type):
		return 0.0
	var timer: SceneTreeTimer = _power_up_timers[effect_type] as SceneTreeTimer
	if timer == null:
		return 0.0
	return timer.time_left


func _get_player() -> Player:
	var scene: Node = get_tree().current_scene
	if scene == null:
		return null
	return scene.get_node_or_null("Player") as Player
```

- [ ] **Step 2: Verificar flujo completo en juego**

Lanza el proyecto (`F5`). Espera a que caiga un power-up y tócalo con el Player. En `Output` no debe haber errores. Si el efecto es `pu_speed_boost`, el jugador debe moverse más rápido. Espera 8s — el efecto debe revertirse. Para `pu_shield_pulse`: recibe un disparo enemigo — el jugador no debe morir en el primer golpe.

- [ ] **Step 3: Commit**

```bash
git add autoloads/run_manager.gd
git commit -m "feat(M20): RunManager manages active_power_ups with SceneTreeTimers, expiry reverts Player effects"
```

---

## Task 9: Actualizar HUD con panel de power-ups activos

**Files:**
- Modify: `scenes/ui/HUD.tscn`
- Modify: `scenes/ui/hud.gd`

- [ ] **Step 1: Añadir `PowerUpPanel` a `HUD.tscn`**

Añadir el nuevo nodo al final del archivo `HUD.tscn` (después del último `[node]`):

```
[node name="PowerUpPanel" type="HBoxContainer" parent="."]
anchor_top = 1.0
anchor_bottom = 1.0
offset_left = 14.0
offset_top = -56.0
offset_right = 500.0
offset_bottom = -14.0
theme_override_constants/separation = 8
```

- [ ] **Step 2: Reescribir `hud.gd` completo**

```gdscript
extends CanvasLayer

const ENCOUNTER_END: float = 45.0

## Color por effect_type — mirrors PowerUpPickup.EFFECT_COLORS
const EFFECT_COLORS: Dictionary = {
	&"pu_rapid_fire":   Color(1.0, 0.5, 0.0),
	&"pu_speed_boost":  Color(0.0, 1.0, 1.0),
	&"pu_shield_pulse": Color(0.2, 0.6, 1.0),
	&"pu_scrap_magnet": Color(0.8, 1.0, 0.2),
	&"pu_wave_amp":     Color(1.0, 0.2, 1.0),
}

@onready var _timer_label: Label = $TimerLabel
@onready var _score_label: Label = $ScoreLabel
@onready var _run_complete_label: Label = $RunCompleteLabel
@onready var _power_up_panel: HBoxContainer = $PowerUpPanel

## Entradas activas: effect_type → VBoxContainer
var _power_up_entries: Dictionary = {}


func _ready() -> void:
	RunManager.score_changed.connect(_on_score_changed)
	EventBus.power_up_collected.connect(_on_power_up_collected)
	EventBus.power_up_expired.connect(_on_power_up_expired)


func _process(_delta: float) -> void:
	var director: Node = get_parent().get_node_or_null("EncounterDirector")
	if director != null:
		var elapsed: float = float(director.get("_elapsed"))
		var remaining: float = maxf(ENCOUNTER_END - elapsed, 0.0)
		_timer_label.text = "T  %02d" % int(remaining)

	for effect_type: StringName in _power_up_entries:
		var entry: VBoxContainer = _power_up_entries[effect_type] as VBoxContainer
		var time_label: Label = entry.get_node_or_null("TimeLabel") as Label
		if time_label == null:
			continue
		var time_left: float = RunManager.get_power_up_time_left(effect_type)
		if time_left > 0.0:
			time_label.text = "%ds" % int(ceilf(time_left))
		else:
			time_label.text = "∞"


func show_run_complete() -> void:
	_run_complete_label.visible = true
	_timer_label.text = "T  00"


func _on_score_changed(new_score: int) -> void:
	_score_label.text = "SC  %d" % new_score


func _on_power_up_collected(data: UpgradeData) -> void:
	if _power_up_entries.has(data.effect_type):
		return  # ya activo — no duplicar entrada
	var entry: VBoxContainer = VBoxContainer.new()
	entry.name = str(data.effect_type)

	var rect: ColorRect = ColorRect.new()
	rect.custom_minimum_size = Vector2(28.0, 28.0)
	rect.color = EFFECT_COLORS.get(data.effect_type, Color.WHITE)
	entry.add_child(rect)

	var label: Label = Label.new()
	label.name = "TimeLabel"
	label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	label.add_theme_font_size_override("font_size", 13)
	label.text = "∞" if data.duration < 0.0 else "%ds" % int(data.duration)
	entry.add_child(label)

	_power_up_panel.add_child(entry)
	_power_up_entries[data.effect_type] = entry


func _on_power_up_expired(data: UpgradeData) -> void:
	if _power_up_entries.has(data.effect_type):
		(_power_up_entries[data.effect_type] as VBoxContainer).queue_free()
		_power_up_entries.erase(data.effect_type)
```

- [ ] **Step 3: Verificar en juego**

Lanza el proyecto. Al recoger un power-up debe aparecer un cuadrado de color en la esquina inferior izquierda con el countdown decreciendo. Al expirar, desaparece. Para `pu_shield_pulse` (duración -1) debe mostrar "∞".

- [ ] **Step 4: Commit**

```bash
git add scenes/ui/HUD.tscn scenes/ui/hud.gd
git commit -m "feat(M20): HUD PowerUpPanel shows active power-ups with color indicators and countdown timers"
```

---

## Task 10: Integración final y verificación de criterios de aceptación

**Files:** ninguno (verificación)

- [ ] **Step 1: Verificar criterio — drops con 15% de probabilidad**

Lanza el proyecto. Elimina ~20 enemigos. Espera ver entre 2 y 5 drops aproximadamente (15% ≈ 1 de cada 7). Si no aparece ninguno, comprueba que `LootTable.try_drop()` se llama en `enemy_base.gd:66`.

- [ ] **Step 2: Verificar criterio — auto-collect sin input**

Deja que un power-up se acerque al Player sin mover el personaje. Debe recolectarse automáticamente al contacto.

- [ ] **Step 3: Verificar criterio — efecto velocidad**

Recoge `pu_speed_boost` (cuadrado cyan). El Player debe moverse notablemente más rápido. Espera 8s — debe volver a la velocidad normal.

- [ ] **Step 4: Verificar criterio — escudo absorbe 1 impacto**

Recoge `pu_shield_pulse` (cuadrado azul). Recibe un disparo enemigo — el Player **no muere**. Un segundo disparo debe matar al Player si hp <= 1.

- [ ] **Step 5: Verificar criterio — HUD countdown**

El panel inferior izquierdo muestra iconos de color con números decreciendo. `pu_shield_pulse` muestra "∞". Al expirar, el icono desaparece.

- [ ] **Step 6: Verificar sin regresiones**

El juego completo (desde TitleScreen → ShipSelect → gameplay → muerte → ResultScreen) funciona sin crashes ni errores en `Output`.

- [ ] **Step 7: Commit final de integración**

```bash
git add .
git commit -m "feat(M20): complete Power-Ups In-Run system — drops, pickup, effects, timers, HUD"
```

---

## Notas de Arquitectura para M21/M22

- **Para añadir pesos por heat level:** Modificar solo `LootTable.try_drop()`. Añadir parámetro `heat_level: int = 0` y reemplazar `randi() % _POWER_UP_PATHS.size()` por lógica ponderada. `EnemyBase` no requiere cambios.
- **Para más power-ups:** Añadir `.tres` en `resources/upgrades/`, añadir a `_POWER_UP_PATHS` en `LootTable`, añadir casos `match` en `Player.apply_powerup/revert_powerup`, añadir color en `EFFECT_COLORS`.
- **Para sprites reales (M21):** Reemplazar `ColorRect` en `PowerUpPickup.tscn` por `Sprite2D` y asignar texture desde `UpgradeData.icon`. Sin cambios en la lógica.
